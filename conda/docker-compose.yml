version: '3'
services:
  conda:
    working_dir: /root
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./repo:/root/repo
      - ./recipes:/root/recipes
      - ./recipe-script.sh:/root/recipe-script.sh
      - ./build-one.sh:/root/build-one.sh
    environment:
      version:
      branch: