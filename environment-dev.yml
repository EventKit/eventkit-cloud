name: eventkit-cloud
channels:
  - local
dependencies:
  - python=3.8
  - postgresql
  - eventkit-cloud=1.8.0
  - memcached=1.6.5
   # The eventkit-cloud recipe is in conda/recipes/eventkit-cloud/meta.yaml.
  - pip:
    - -r requirements-dev.txt
