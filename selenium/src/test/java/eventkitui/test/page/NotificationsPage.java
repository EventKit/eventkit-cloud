package eventkitui.test.page;

import eventkitui.test.page.core.PageObject;
import org.openqa.selenium.WebDriver;

// TODO - This is the notifications popup window that can be accessed via the top panel.
public class NotificationsPage extends PageObject {
    public NotificationsPage(WebDriver driver) {
        super(driver);
    }
}
